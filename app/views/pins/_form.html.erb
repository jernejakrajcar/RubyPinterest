<div class="container">

  <div class="login-form">
    <div class="main-div">
      <div class="panel">
        <h2 class="form-heading">New Pin</h2>
      </div>
      <%= form_for(@pin, html: { class: 'directUpload', data: { 'form-data' => (@s3_direct_post.fields), 'url' => @s3_direct_post.url, 'host' => URI.parse(@s3_direct_post.url).host } }) do |form| %>
        <% if @pin.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

            <ul>
              <% @pin.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="field">
          <%= form.label :title %>
          <%= form.text_field :title, required: true, class:"form-control" %>
        </div>

        <div class="field">
          <%= form.label :description %>
          <%= form.text_area :description, required: true, class:"form-control" %>
        </div>


        <div class="field">
          <% if @pin.image.attached? %>
            <%= image_tag @pin.image %>
          <% end %>

          <%= form.label :image %>
          <%= form.file_field :image, class:"form-control-image" %>
        </div>

        <div class='bar'></div>

        <div class="field">
          <%= form.label :board %>
          <%= form.select :board, options_from_collection_for_select(current_user.boards, "id", "name") %>
        </div>


        <div class="actions">
          <%= form.submit "Submit", class:"btn btn-danger" %>
        </div>
      <% end %>

    </div>
  </div>
</div>
