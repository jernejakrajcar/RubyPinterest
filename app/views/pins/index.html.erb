

<%# <% @pins.each do |pin|  %>
  <%# <% if pin.image.attached? %>
    <%# <%= h2=link_to image_tag(pin.image), pin %>
  <%# <% end %>

<%# <% end %>

<div id="pins" class="transitions-enabled infinite-scroll centered clearfix">

  <% @pins.each do |pin| %>
  <div class="box panel panel-default">

    <% if pin.image.attached? %>
      <%= link_to (image_tag(pin.image)), pin %>
    <% else %>
    <h2 class="pin-link">
      <%= link_to pin.title, pin %>
    </h2>
    <% end %>


  </div>
  <% end %>
</div>

<div class="add-pin">
<%= link_to '+', new_pin_path, class:"button-add" %>
</div>

<!-- <table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Author</th>
      <th>Description</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%# <% @pins.each do |pin| %> %>
      <tr>
        <%# <td><%= pin.title %></td> %>
        <%# <td><%= pin.user.name || 'Anonimous' %></td> %>
        <%# <td><%= pin.description %></td> %>
        <%# <td><%= image_tag(pin.image) if pin.image.attached? %></td> %>
        <%# <%= display_index_pin_links(pin) %> %>
      </tr>
    <%# <% end %> %>
  </tbody>
</table> -->

<br>
